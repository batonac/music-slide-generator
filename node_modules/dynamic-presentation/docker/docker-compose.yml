version: '2.2'

services:
  web:
    image: node:20.11.1-bookworm
    ports:
      - ${WEB_PORT}:8080
    volumes:
      - ..:/var/www/
    command: 'npm run setupAndServe'
    working_dir: /var/www/
    restart: always
