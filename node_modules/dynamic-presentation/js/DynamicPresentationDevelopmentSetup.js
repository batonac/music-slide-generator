/* globals Reveal */

// See https://book.verovio.org/first-steps/getting-started.html
// Only start doing stuff once the verovio is loaded
document.addEventListener('DOMContentLoaded', (event) => {
  main();
});

function main () {
  // This function's setup is specific to the dynamic-presentation dart site (and should not be loaded on My.Hymnary)
  // Set to the default aspect ratio (see slides.js in My.Hymnary for more info)
  Reveal.configure({ width: 1024, height: 768 });
  // TODO: Mimic slides.js in other ways?
}
